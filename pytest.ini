[pytest]
# Lightweight test config for the Python (backend) half of Todoister.
# `pythonpath = .` lets tests `import paths` / `import sync` etc. from the triage root.
testpaths = tests
pythonpath = .
